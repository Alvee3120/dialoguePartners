# Taste
- Prefers building websites with Next.js when a project is frontend-only. Confidence: 0.9
- Prefers starting with a basic design first and improving/refining the design in later iterations, rather than polishing in the first pass. Confidence: 0.9
- Prefers incremental, review-gated delivery: build one part/section, hand it over for the user to check, and only continue when the user approves — never the whole site at once. Confidence: 0.9
- Prefers image placeholders that visibly state which image goes where (path, subject, crop) over sourcing or embedding real photos. Confidence: 0.9
- Prefers to supply site content piece-by-piece in later messages; build pages from that provided copy rather than inventing content. Confidence: 0.8
- When the user provides a detailed, structured content specification (numbered sections covering every page with headings, eyebrow labels, body copy, CTAs, form fields, and contact details), expects that provided copy to be implemented faithfully and verbatim across all routes/data — including carrying the exact eyebrow labels, section headlines, intro lines, and CTA buttons into the components — rather than paraphrasing or restructuring. Confidence: 0.8
- For unspecified fields in a content spec (e.g. advisor roles, focus areas, bios not provided by the user), is comfortable with the agent drafting reasonable placeholder copy and clearly flagging it as an adjustable draft for later confirmation. Confidence: 0.7
- When supplying exact copy, wants it reproduced byte-for-byte and does NOT want it "polished": no grammar fixes, no title-casing/headings changes, no wording tweaks — even when the original contains grammatical errors or lowercase sentence starts (responds to such edits with just "exact"). Any suggestions about corrections should be raised as optional notes, not applied. Confidence: 0.9
- Expects a full multi-page architecture from the start (home = overview linking out → dedicated About, Insights with per-pillar detail pages, People with individual profiles, Contact), not a single-page landing — even when the brief only names "home page sections." Confidence: 0.9
- When content areas are described as "others will be added by you," expects the agent to proactively scaffold those pages/routes with placeholder content in the current iteration rather than deferring them until asked. Confidence: 0.8
- When asked to replicate a reference design image, expects the layout, spacing, colors, and typography to match the reference exactly ("follow the exact same design", "make sure the design and colour exactly look the same"), not just loosely resemble it. Confidence: 0.9
- When a reference design/theme conflicts with the project's own branding/content, prefers to match the reference's look and structure but keep the project's own branding and copy in those slots ("match look, keep our content"), rather than reproducing the reference's text/content verbatim. Confidence: 0.9
- For images that will be supplied later, prefers clean neutral gray placeholder boxes that are easy to swap for real assets over stylized mockups that mimic the reference's imagery. Confidence: 0.9
- Prefers replacing static placeholder images with real media (e.g. background video) in hero sections, using autoplay + loop + muted + playsInline with no controls, layered under a dark semi-transparent overlay (plus a vertical gradient) to keep text legible. Confidence: 0.8
- Prefers the overlay/scrim over background hero media to be light enough that the underlying video/image stays clearly visible — asks to lower the overlay opacity ("make the video more visible") rather than keeping heavy dark tints for text contrast, and for section background images is happy to drop the white overlay/gradient entirely ("no need to use the bg overlay"), expecting the overlay div and all its CSS rules (base, desktop, mobile) removed rather than just toned down. The same instinct extends beyond heroes to image cards: asks to remove the gradient overlay from value cards ("do not need the overlay") so the photo shows unobstructed, accepting the resulting contrast risk for the white text laid over it. Confidence: 0.6
- Hosts site assets (images, video) on R2 and supplies them as remote URLs (e.g. hero video, footer background image); once provided, expects them wired into the site data (e.g. a `site` data constant) and rendered with Next.js Image (configuring `remotePatterns` in next.config for the R2 host) rather than keeping placeholder slots or local copies. Also prefers asset fixes to happen at the source: when a supplied file is unusable on the web (e.g. a HEIC that only Safari decodes), re-exports/re-uploads a web-friendly format to R2 and has the agent swap the URL — rather than the agent converting the file locally and adding image binaries to the repo. Confidence: 0.85
- When asked to "use the related img" for sections but has not yet provided real photos, prefers the agent to actually source theme-matched stock images from the internet and wire them into each section now (rather than leaving gray placeholder boxes) — treating them as temporary stand-ins the user will swap out later. Confidence: 0.8
- When sourcing stock images for sections, cares that each image genuinely matches its section's subject ("related"); expects the agent to verify the actual content of an image before wiring it in rather than trusting a guessed URL/filename, and to self-host the downloaded files in the project (`public/images/`) so the site no longer depends on the third-party host. Confidence: 0.8
- Prefers detail/profile pages to be built "in details" and "more graphical" — rich multi-section layouts with icon-backed competency cards, timeline, numbered highlight lists, and photo galleries — over minimal/simple layouts. Confidence: 0.8
- When building a person's profile page, wants a substantial number of image placeholders (explicitly ~10+) set up for real photos the user will drop in later, rather than just one portrait slot. Confidence: 0.9
- Dislikes fabricated filler text in the UI: when real captions/descriptions aren't available, prefers them omitted entirely (render the caption only when a real description exists) rather than displaying generated placeholders like "Name — photo 01". Conversely, when they do supply real caption copy with a set of images (e.g. "Add caption At European office – Ambassador Charles Whiteley"), expects it attached to that gallery entry as its description and visible on the rendered tiles/lightbox. Confidence: 0.7
- Prefers media/press sections (e.g. a profile's "Media & Interviews") rendered as image-card grids — each card pairing a photo thumbnail with a title and a link — rather than plain text link rows. Confidence: 0.6
- For server/infrastructure changes, wants nothing executed before explicit approval: asks the agent to "ask me first before executing anything" and to gate each step on confirmation rather than acting autonomously. Confidence: 0.9
- Cares strongly about not disrupting existing systems when working on shared infrastructure (e.g. hosting a new site on a server that already runs another live site): expects changes scoped to the new app only — no edits to existing configs/services — and a verification that the pre-existing site stays healthy. Confidence: 0.9
- When doing deployment/server setup, prefers being walked through it step by step with exact commands and explanations, rather than the agent completing everything on its own. Confidence: 0.8
- For hosting, prefers to deploy early to a managed platform (e.g. Vercel) to get a quickly shareable/reviewable URL first, and migrate to their own server only after the project is complete. Confidence: 0.6
- Wants the site header/nav bar to sit *on* the hero section (overlapping full-bleed hero) rather than in the page flow above it — the transparent-header-over-hero look, not a bar that pushes the hero down. Confidence: 0.75
- Wants the hero section to span the full viewport height ("100 vh") rather than a fixed pixel min-height. Confidence: 0.6
- Prefers replacing copy/prose-heavy content sections with graphical, visual content (e.g. diagrams with connected nodes, icon-backed cards) — repeatedly asks for "graphical things" / "more graphical" over walls of text. Confidence: 0.6
- Prefers full-width layout over side-by-side splits for media/content sections: media/image blocks that replace full sections should span the full width of the page container (not a narrower centered max-width), and when a section has a side image he asks to drop the two-column split — make the copy span the full container width and place the image full-width below the content instead of beside it. Confidence: 0.65
- Dislikes unused empty space beside section content — flags sections where text/headings are constrained by a max-width (`max-w-3xl`, `max-w-2xl`) as leaving too much room on the right side, and expects those constraints removed so copy spans the full container width. Reiterated for the pillar detail page intro paragraphs, which lived inside the 768px heading column and had to be pulled out as a sibling block to span the container (up to ~1216px) — the agent should not over-constrain body copy to a narrow column by default. Confidence: 0.75
- When asking the agent to describe/explain something (e.g. "write the flow for the diagram"), wants plain content-level text describing what it depicts/means, not a technical implementation walkthrough (layout math, CSS classes, component internals). Confidence: 0.6
- When the plain-text description is requested, expects it to be substantive and informative — wants richer prose that also covers the wider company overview/positioning context (drawn from the project's existing data/content), not a terse one-line caption. Confidence: 0.6
- When adapting a reference design onto an existing page, expects only the page body to be built: the reference's global chrome (header/footer) should be skipped in favour of the project's own shared components, along with any other sections the user explicitly excludes (e.g. team members). Confidence: 0.6
- Uses the client logo's colours (Dialogue Partners) for all accent styling, with fixed hex values and role-based application: navy #173A5E as the deep accent (accent text, links, icon glyphs, focus outlines), teal #339B9E as the primary accent (buttons, highlights on dark surfaces), and light teal #DCEFEF for soft accent backgrounds (icon circles, button hovers, focus rings). Confidence: 0.85
- Does not want gold/amber accents anywhere in the palette. Confidence: 0.85
- Prefers compact icon+heading cards in feature/values grids, with the descriptive paragraph removed entirely rather than shortened (explicitly: "just keep the heading, no description"). Exception: when replicating a reference card design that clearly shows body copy under the heading, keeps/restores the descriptive paragraph (from existing data — no invented text). Also strips decorative CTA affordances from cards — asked for the circular trailing-arrow button to be removed so a card holds only image, badge, heading, and text (later edits move toward leaner cards, not richer ones). Confidence: 0.6
- Wants "dark" surfaces/sections to use the brand logo navy (#173A5E) instead of black or near-black — the brand colour should carry through backgrounds, overlays/scrims, and dark bands, not just accents. Confidence: 0.8
- Wants related cards/boxes on a page grouped so they can all be seen together at once (within a single viewport / above the fold) — prefers tightening headers, padding, and cell content to fit them in, rather than tall separated blocks that require scrolling. Confidence: 0.6
- Works from versioned, structured content briefs (labelled "v2 — Updated") supplied as the project's single source of truth: a new version supersedes the earlier draft, and expects the whole implementation to be reconciled to it — including hunting down stale hardcoded references sitewide (e.g. "four pillars" strings, counts in stats/FAQs/metadata, renamed nav labels) rather than only editing the primary data file. Confidence: 0.7
- When the agent surfaces clarifying decisions with a clear recommended default, tends to accept the recommended options as a batch rather than over-specifying. Confidence: 0.6
- When asking for a piece of content (e.g. a stats/"by the numbers" block, or an FAQ section) to be removed — even "for now" — expects it removed everywhere it appears across the whole site: a site-wide search for all renderings of the section/data, deletion of the section component and its now-orphaned data file, and cleanup of the unused imports/exports/types, rather than just deleting the visible block on one page. This applies to individual fields/values too, not just whole sections: told a specific string "i dont want this anywhere", expects the field dropped from the data type and every entry AND the render fallback that surfaces it removed (not just the one occurrence the agent guessed), with a site-wide grep confirming no trace remains. Confidence: 0.75
- Prefers header nav submenus (e.g. the "Insight" item listing the pillars) to open on hover rather than on click. Confidence: 0.55
- Wants menu/dropdown entries to fit on a single line each: long labels must not wrap, so the submenu panel should size to its widest item (e.g. w-max + whitespace-nowrap) rather than being forced to a fixed narrow width. Confidence: 0.5
- Prefers a geometric sans-serif typeface for headings (switched from the serif Playfair Display to Urbanist), with a single family driving both body and headings rather than a serif/sans pairing. Confidence: 0.65
- Prefers flat, solid section backgrounds in alternating light bands (white / paper) with a single dark navy band, over gradient washes and decorative blob shapes. Confidence: 0.6
- Prefers subtly rounded card corners (~10-12px, `rounded-xl`) over heavily rounded ones (~24px, `rounded-3xl`). Confidence: 0.6
- Prefers pill-shaped buttons with a trailing arrow, defined once as a shared class helper/variant set rather than repeated inline button classNames. Confidence: 0.55
- Prefers a wide content column — container max-w-7xl rather than a narrower max-w-6xl. Confidence: 0.5
- Iterates on media (images or video) by sending just a new R2 URL ("replace the img with …", "use this video for hero, remove old one") and expects a minimal, scoped swap of that single asset reference — keep the surrounding layout, sizing, and code as-is, verify the new URL resolves, and don't bundle in other changes. Confidence: 0.7
- When changing the styling/colour of a repeated element (e.g. the "Our Philosophy" block, brand motto, or advisor cards), expects it applied consistently across every place it appears on the site ("change it in whole website"), not just the page shown/mentioned — "everywhere" is the default expectation for visual/branding edits, the same way removals must be site-wide. Confidence: 0.75
- Prefers hero sections kept lean — just the eyebrow, main heading, supporting line, and CTAs — and does not want a numeric stats/metric strip (e.g. "40+ years", "5 pillars", "4 stakeholder groups") or decorative affordances like the animated "Scroll to Explore" mouse indicator inside the hero (asks for these to be removed outright). Confidence: 0.55
- Strongly does not want images cropped: images should render at their natural aspect ratio and full height rather than being forced into a fixed ratio. Expects `width`/`height` from the intrinsic dimensions with `w-full h-auto` (and rounded corners via class), not `fill` + `object-cover` inside an `aspect-[x/y]` box. Same reaction to a fixed pixel-height media box (e.g. `h-52 sm:h-56`) with `object-cover` on wide cards — flags the photo as "cropped / less visible" and expects the box sized to the source ratio (e.g. `aspect-[3/2]`) so the whole image shows, even if cards get taller. Confidence: 0.75
- Wants every card in a repeated grid to end up the same height: when captions/descriptions vary in length, expects space reserved so shorter cards don't shrink — e.g. reserving a fixed two-line caption box for all tiles ("for all photo keep the two line space, so that all card look same height") rather than letting each card size to its own caption. Confidence: 0.6
- When supplied caption copy contains an explicit line break, expects it rendered on separate lines rather than collapsed into one paragraph (e.g. `whitespace-pre-line`, with the newline stored in the data). Confidence: 0.5
- Values vertical alignment of media across a row: when different card widths make images render at different heights (e.g. a 2/3-wide card next to 1/3 cards), asks for the image panel height to be pinned to match the rest (e.g. a fixed lg height with `object-cover`), accepting the resulting crop — overriding the general natural-aspect instinct when row alignment is at stake, and expected to apply only at the breakpoint where the widths actually differ while smaller breakpoints stay untouched. Confidence: 0.5
- Distinguishes text colour from background colour in styling requests: "make the text colour yellow" means the text/label only, not the section's background surface or fill, and reacts sharply ("undo it, i said…") when the agent recolours backgrounds instead. Expects styling instructions interpreted literally and tightly scoped (e.g. "only" for one specific element). Confidence: 0.65
- Wants the site logo to sit at the exact horizontal center of the header nav bar, not just visually near it — expects the layout to equalize the left/right nav groups (e.g. three-column grid) so the logo is truly page-centered regardless of how wide the nav groups are. Confidence: 0.6
- Expects visual consistency across analogous components: when one component's styling is established (e.g. the accent icon-badge treatment `bg-accent-soft text-accent-deep` on the "Our Values" cards), wants other similar card/component variants recoloured to match it rather than keeping their own variant (e.g. `bg-gold text-navy` on the "What We Do" badges), and names the existing component as the source-of-truth reference for such requests. The request is framed relative to that component ("rounded as our style", "a little bit bigger like media and interview") rather than as absolute values, and covers geometry/typography as well as colour — e.g. restyling the photo-gallery tiles into cards that share the media cards' rounding, border token, white background, padding and hover, with the caption bumped to the same size/weight and moved into its own box inside the card (tight vertical stack) instead of sitting loose under the image. Confidence: 0.65
- Dislikes excessive vertical whitespace/padding: flags sections with large top padding (e.g. `pt-28 sm:pt-36` stacking on top of the sticky header) as "too much padding" and expects it removed so content starts right under the header, offering to reintroduce a small amount if it feels tight. Also asks for whole sections to be made "thinner" — cuts section vertical padding (e.g. `py-20 sm:py-28` → `py-12 sm:py-16`) and trims media-panel min-heights, and prefers removing decorative slogan/tagline copy from a section rather than keeping it as filler. Confidence: 0.6
- When adapting a reference card/design image, borrows the reference's structure/layout but substitutes the project's own typography, colour palette, and design tokens — "do it like that but follow our typography, colour and design" — rather than reproducing the reference's own fonts, colour scheme, or styling idiom. Confidence: 0.8
- When the number of items in a repeated grid/collection shrinks (e.g. down to a single client logo in the Clients section), expects the layout reworked so the remaining item(s) are presented nicely — centered and sized up — rather than left sitting in the original multi-column grid. Confidence: 0.5
- When a section accumulates several images, prefers them shown in a rotating carousel/slideshow (auto-advancing with prev/next and dot controls) rather than a static grid/collage — new images are meant to be added into the same carousel alongside the existing ones. Confidence: 0.5
- Expects such an image carousel to show multiple slides at once (e.g. 3 images visible on desktop, fewer on smaller breakpoints/one on mobile) rather than one image filling the frame, flagging a single-image view as wrong ("there will be showing 3 images at a time, now its showing only one"). Confidence: 0.55
- For a card that links out (e.g. a "Media & Interviews" card), wants multi-image support added without changing the card's look ("the card will be same"): when an item has multiple images it becomes a carousel with two small left/right chevron arrow buttons overlaid on the image and no pagination dots, while items with a single image render exactly as before with no controls. The arrows stay user-controllable, but auto-advance is also wanted on top: after the arrows were built, asked to "add auto image change after 3 second for every cards" — so such cards auto-rotate on a ~3s interval in addition to the arrows, and auto-advance (not manual-only) is the default for multi-image cards, consistent with the auto-scrolling galleries elsewhere. Confidence: 0.65
- Wants newly added items kept at the top of their list/collection ("keep it 1st") — newest content first, rather than appended at the end. Confidence: 0.5
- For auto-rotating image carousels/cards, specifies the interval explicitly (3 seconds) and expects it to apply uniformly to every card with multiple images, independent per card. Confidence: 0.5
- For image carousels, prefers continuous, seamless auto-scroll ("auto scroll loop wise / continuous scroll") — an infinite marquee that never stops — over stepped slide-by-slide advance with prev/next arrows and dot controls (which get removed once continuous scroll is used). Confidence: 0.55
- Prefers the header nav links horizontally centered in the nav bar — logo on the left, links in the middle, CTA on the right (e.g. a `grid-cols-[1fr_auto_1fr]` layout) — rather than the nav grouped next to the logo on the left. Confidence: 0.55
- When adding a nav item for a section that doesn't exist yet, expects a placeholder route/page created in the same pass (an explicit "create a blank page, will work later" is acceptable) rather than shipping a dead link. Confidence: 0.6
- Wants a call-to-action band to be merged into the footer as one continuous visual block ("add with the footer"), not a separate standalone section: the two must share the exact same background colour at the junction (no visible seam — the footer overlay starts fully opaque to match the band above) with padding trimmed so they read as a single unit. Confidence: 0.55
- When a new section needs background imagery, is happy to reuse an existing branded asset already in the project (e.g. "use the footer image for this section") rather than sourcing or adding a new image — expects the existing asset (referenced via the site data constant / Next Image) wired into the new section. Confidence: 0.5
- Expects every section to be responsive on mobile — explicitly asks the agent to "make sure it is responsive in mobile", so layouts must degrade properly at narrow breakpoints (e.g. horizontal rows collapse/stack, side-by-side items become vertical) rather than letting the desktop layout leak into phones. Confidence: 0.7
- Responsive-on-mobile does not always mean stacking: for a compact horizontal strip of items (e.g. the hero stat strip — "100+ Industry Stakeholders / Strategic Insights / Sustainable Growth"), wants it to stay on ONE row on mobile ("i want this in line for mobile") — achieved via flex-nowrap + justify-between with tighter gaps and smaller icon/text sizes at the base breakpoint, reverting to the original roomier spacing at `sm` and up — rather than wrapping each item onto its own line. Confidence: 0.55
- Same instinct for multi-column page regions: on mobile the default should be to keep columns side by side (e.g. footer "Explore" and "Pillars" in a 2-column grid, with remaining blocks like logo/Contact spanning full width and gaps tightened) rather than letting every child stack vertically, while the `md`/`lg` layouts stay unchanged. Confidence: 0.6
- When the agent's own take on a section "doesn't look nice," hands over ready-made raw HTML markup + CSS for that section as the design reference and expects it ported in essentially 1:1 (same class structure, values, pseudo-elements/`::before`/`::after`, gradients, and desktop-first breakpoints) rather than reinterpreted in the project's own idiom — a preferred handoff format for pinning down a look the agent's guess missed. Confidence: 0.7
- For wide, full-bleed photo/banner sections, wants the copy block centered — both horizontally and text-aligned center — rather than anchored to one side (explicitly "keep the text centr[ed]", in response to a banner whose copy was right-anchored to sit over the image's open area). Confidence: 0.5
- Prefers the project's own logo asset (e.g. `public/images/logo.svg`) over third-party/generic icons (e.g. a Phosphor `Leaf`) for decorative brand imagery such as watermarks — expects the swap to keep the exact same placement/sizing treatment as the element it replaces ("the way you use the leaf"). Confidence: 0.55
- When flagging a visual defect, pairs the fix request with a "what's that?" — expects the agent to diagnose and explain the actual root cause (e.g. a stray pixel line baked into the image asset rather than anything in the CSS) rather than just patching the nearest plausible cause. Confidence: 0.5
- Prefers deliberate asymmetric column proportions in multi-card grid rows over equal halves: when a two-card row sits beneath a three-column row, wants the cards sized 33% / 66% (explicitly "first row contains 3 coloum (each 33% — which is good), second row contains 2 coloum (you design 50% each now, but i want it 33% and 66%)"), i.e. an fr/col-span split that lines up with the grid above rather than a flat `grid-cols-2`. Gives layout feedback as exact percentage targets and expects the rendered result to hit them. Confidence: 0.6
- Works in the IDE alongside the agent: makes their own edits to the same files (including uncommitted URL/content swaps and editor-applied whitespace changes) while the agent is working, so the working tree may differ from the last-read state and changes shouldn't be assumed to originate from the agent. Confidence: 0.5
- Distinguishes the scope of visual edits from content edits: branding/styling changes should go site-wide, but content/asset changes must stay scoped to the surface asked about — when two surfaces share one data source (the home "Our Values" cards + the `/values` page grid, or the home "What We Do" cards + the `/pillars` page grid), swapping/adding images for one page must not change the other, and the other page's cards should be restored to their previous image set (prefers the shared field split into per-surface fields — e.g. `image` for home vs a separate `pageImage` field — over mutating one shared source; states the pages "are individual"). Confidence: 0.75
- When an image must be cropped to fit a fixed-height box (`object-cover`), wants the crop anchored to the top (`object-top`) so the upper part of the picture stays visible instead of being trimmed evenly top and bottom — actively resists the default center crop ("make the img top center, so that top part visible"), scoped to the desktop breakpoint where the crop actually applies. Confidence: 0.5
- Treats component/data variants as genuinely distinct and says so explicitly ("i have 3 types of values card, and three is different") — expects the different card types kept as separate implementations rather than collapsed into one shared component, and repeats this correction when the agent's earlier fix targeted the wrong copy. Confidence: 0.5
- Wants detail/profile URLs derived from human-readable names rather than placeholder IDs: person pages should slugify to kebab-case of the person's name (e.g. `/people/ziaur-rahman`), not `advisor-1`/`advisor-2`, so URLs read naturally. Confidence: 0.65
- Wants photo-gallery tiles to open a full-screen lightbox on click (dark backdrop, close button, Escape/click-outside to dismiss, caption shown beneath) rather than linking anywhere — explicitly "no need to link", a click should just show the image larger. Confidence: 0.55
- Expects a feature built on one surface to be extended to analogous surfaces for behavior parity, not only styling consistency: after the multi-image carousel was built for the media/interview cards, asked for "the same feature for multiple images" in the Photos section. Confidence: 0.5
- Prefers carousel arrow/navigation controls hidden by default and revealed on interaction ("make the arrow button hidden by defult ... after hover it will show"), for every section that has them ("for both section") rather than arrows permanently visible overlaid on the image. Confidence: 0.5
- Objects to repeated grids/tile collections whose layout changes shape as items are added: flagged the photo gallery as "layout broke" when a 4th entry flipped on a special first-tile/hero treatment (row-span, cropped into a tall strip). Prefers every tile rendered identically so the layout stays stable no matter how many entries exist. Confidence: 0.5
- Expects supplied media to be verified as actually LOADING and displaying for end users — not merely that the URL returns a file ("make sure [the images] load successfully"). Wants the agent to surface browser-compatibility problems (e.g. HEIC only decodes in Safari, so it renders broken in Chrome/Edge/Firefox) and confirm every asset truly renders, rather than assuming a 200 response means the image works. Confidence: 0.6
- For a new full-stack feature, prefers a self-hosted, transparent stack they fully control for production — Postgres on their own VPS, custom email/password auth, and their existing Cloudflare R2 bucket for uploaded files — over managed all-in-one services: explicitly rejected Supabase (storage + built-in auth) and chose "own VPS / Node server" over the recommended Vercel option. Confidence: 0.6
- For back-office/admin access, prefers proper account-based auth (email + password accounts, extensible to multiple admins later) over a single shared password. Confidence: 0.5
- Prefers operational notifications (e.g. a new job application arriving) handled inside the app via an admin dashboard/inbox rather than integrating a third-party email service (declined the "email alert" option in favour of dashboard-only). Confidence: 0.5
- Once a plan is approved, is comfortable having the agent build a whole batch of feature phases (e.g. auth, CRUD, public pages, inbox) in one pass before reviewing — picking the "fewer interruptions" pacing option — with the hard approval gate reserved for live infrastructure, credentials, and destructive steps rather than for code volume. Confidence: 0.55
- Runs separate Cloudflare accounts for development vs production (R2 object storage), and wants assets to be portable between them without re-uploading files or rewriting links: prefers environment-agnostic references — storing object keys in the data and resolving the origin from an env var (e.g. `NEXT_PUBLIC_R2_BASE_URL`) rather than hardcoding absolute `pub-<hash>.r2.dev` URLs — so the same content works against whichever account is configured. Confidence: 0.8
- For promoting DB-backed content (e.g. job posts) from dev to production, wants a dedicated export/import mechanism — a committed, diffable JSON export plus an idempotent import run at deploy time — instead of re-entering or re-uploading data by hand; expects environment-private/personal data (candidate applications and CVs) to be deliberately excluded from such promotion. Confidence: 0.6
- When test/dummy content has to exist on a user-facing surface (e.g. sample job postings shown on the public careers page), wants it explicitly and unmistakably labelled as test data in the title (and supporting copy) so no real user mistakes it for live content or acts on it ("write Testing or dummy with title, so that no one got harassed") — a preference for protecting end users from being misled, not just a one-off naming request. Confidence: 0.5
- For local development, wants the app wired to a locally-created database — when handing over real/remote credentials, explicitly asks to "create db locally and use that" (spin up a local Postgres instance for dev) rather than pointing the dev environment at the remote/production DB, consistent with keeping dev and prod environments separate. Confidence: 0.55
- Wants the text logo lockup stacked one word per line (e.g. "Dialogue" over "Partners") with the tagline placed on a further line beneath, rather than the wordmark running on a single line — and cares that the tagline line spans the same width as the wordmark, with the left/right edges aligned. When the tagline text is much longer than the wordmark, prefers shrinking the tagline font size (down to a few px, e.g. 3.5px) so it covers exactly the wordmark's width while keeping the wordmark at its ORIGINAL size ("the text connecting … will small so that it cover the width Dialogue and Partners takes") — flipping an earlier inclination to instead grow the whole lockup (larger wordmark, taller header, matching icon). Either way, the width match is the priority and small/unusual font sizes are acceptable to achieve it. Confidence: 0.55
- Keeps the project in a GitHub repo (`Alvee3120/dialoguePartners`) and treats it as the canonical "latest code": when resuming work after time away, hands over the repo URL and expects the agent to FIRST sync the local checkout to it (fetch + fast-forward pull) — checking remote/working-tree state before and after — before building anything new. Confidence: 0.5
- Frames copy edits as tightly scoped changes: says "keep [X] same as before just change the text …", so a wording/URL/label swap must leave the surrounding layout, sizing, styling, and component structure untouched — don't use a copy change as an opening to restyle or redesign the element, and revert prior unintended redesigns back to the original look when only the text was requested. But "keep [X] same" scopes to the named element only and is NOT a licence to over-revert: design changes already requested/agreed (e.g. the stacked Dialogue/Partners wordmark) must survive the edit, and restoring every touched file to its committed original draws a sharp correction ("i said do the new style…"). Confidence: 0.6
- Prefers styling discussed in exact numeric values: when asking about an element's look, asks for each part's size "in px" (e.g. which of the logo's wordmark/tagline lines is which size) and expects precise measurements — font sizes, line-heights, letter-spacing, margins — rather than qualitative descriptions or approximations. Confidence: 0.5
